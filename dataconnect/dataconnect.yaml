specVersion: "v1beta"
serviceId: "new-react"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "new-react-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
